const Loader = () => {
  return (
    <div
      style={{
        height: "300px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      Loading...Component
    </div>
  );
};

export default Loader;
